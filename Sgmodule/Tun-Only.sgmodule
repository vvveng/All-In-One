#!name= Tun Only
#!desc= (纯TUN代理并保持高级功能)Enable tun only and force 80 port http proxy

[General]
compatibility-mode = 3
force-http-engine-hosts = %APPEND% -<ip-address>, *

[MITM]
tcp-connection = true